name: fluxy_test
description: Testing utilities and mocks for the Fluxy framework.
version: 0.2.6
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  fluxy:
    path: ../../

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
