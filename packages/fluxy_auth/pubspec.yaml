name: fluxy_auth
description: Authentication plugin for the Fluxy framework.
version: 0.2.6
publish_to: none
environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.10.0'
dependencies:
  flutter:
    sdk: flutter
  fluxy:
    path: ../../
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
